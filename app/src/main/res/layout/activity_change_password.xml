<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".activities.LoginActivity">

    <include layout="@layout/nav_bar"/><!--引用布局-->

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@mipmap/logo"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="@dimen/marginSize"/>

    <xtmusic.xiaotian.com.xtmusic.views.InputView
        android:id="@+id/input_oldpassword"
        android:layout_width="match_parent"
        android:layout_height="@dimen/inputViewHeight"
        app:input_icon="@mipmap/password"
        app:input_hint="请输入原密码"
        app:is_password="true">
    </xtmusic.xiaotian.com.xtmusic.views.InputView>

    <View
        style="@style/line"/>

    <xtmusic.xiaotian.com.xtmusic.views.InputView
        android:id="@+id/input_newpassword"
        android:layout_width="match_parent"
        android:layout_height="@dimen/inputViewHeight"
        app:input_icon="@mipmap/password"
        app:input_hint="请输入新密码"
        app:is_password="true">
    </xtmusic.xiaotian.com.xtmusic.views.InputView>

    <View
        style="@style/line"/>

    <xtmusic.xiaotian.com.xtmusic.views.InputView
        android:id="@+id/input_renewpassword"
        android:layout_width="match_parent"
        android:layout_height="@dimen/inputViewHeight"
        app:input_icon="@mipmap/password"
        app:input_hint="请确认密码"
        app:is_password="true">
    </xtmusic.xiaotian.com.xtmusic.views.InputView>

    <View
        style="@style/line"/>

    <Button
        style="@style/commitBtn"
        android:text="确  定"
        android:layout_marginTop="@dimen/marginSize"
        android:onClick="onChangePasswordClick"/>


</LinearLayout>